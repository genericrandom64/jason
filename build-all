# build tests
make clean all

# update site table
[ "$(whoami)" = "generic" ] && tests/html.out > /tmp/opl; doas chown http:http /tmp/opl; doas -u http mv /tmp/opl /srv/http/computer/opcodes/
cd cpu
./chk.sh
cd ..
